package day0219;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.awt.event.KeyAdapter;
import java.awt.event.KeyEvent;
import java.awt.event.WindowAdapter;

public class LoginEvent extends KeyAdapter {
/*
	private LoginForm lf;

	public LoginEvent(LoginForm lf) {
		this.lf = lf;
	}// LoginEvent

	public void keyReleased(KeyEvent lf) {
		//키보드의 키가 올라올 때 작업
		System.out.println("여기가 커서 있는 곳");
		
		switch(lf.getKeyCode()) {
		case KeyEvent.VK_ENTER : 
			lf.dispose();
		case KeyEvent.VK_COMMA:
			tkd.getJtf().setText(tkd.getJtf().getText()+" " );
			// , 하면 자동으로 한 칸 띄기
		}//end switch
	}//end  keyReleased
	
	
	@Override
	public void keyTyped(KeyEvent e) {
		

		if (lf.getTitle() == null) {// 아이디가 없을때
			lf.getJlblOutput().setText("아이디는 필수!");

		} else {
			// 아이디가 있을 때
			// 비밀번호가 비었을 때
			if (lf.getJpfPass() == null) {

				lf.getJlblOutput().setText("비밀번호는 필수!");
				lf.getJtfId().requestFocus();// 아이디로 돌아감
	
			}
		}*/
	}
	

}
